# 🌐 WebSocket Implementation Guide

## Обзор

Арбитражный сканер использует **WebSocket** для получения данных в реальном времени с обеих бирж:
- **BingX**: Кастомный WebSocket класс (`BingXOrderBook`)
- **MEXC**: WebSocket через библиотеку `ccxt.pro`

---

## 📋 Требования

```bash
pip install ccxt[pro]>=4.0.0
pip install websockets>=12.0
pip install python-dotenv>=1.0.0
```

---

## 🚀 Быстрый старт

```bash
# 1. Установить зависимости
pip install -r requirements.txt

# 2. Запустить сканер
python arbitrage_scanner_ws.py
```

---

## 🔧 Архитектура WebSocket

### 1. BingX WebSocket

**Файл:** `tools/bingx_orderbook_live.py`

**Класс:** `BingXOrderBook`

**Особенности:**
- ✅ Нативный WebSocket (библиотека `websockets`)
- ✅ Подписка на `BTC-USDC@depth5`
- ✅ Сжатие данных через gzip
- ✅ Автоматический reconnect
- ✅ Методы: `get_best_bid()`, `get_best_ask()`

**Пример использования:**
```python
from bingx_orderbook_live import BingXOrderBook

bingx = BingXOrderBook(symbol="BTC-USDC", depth=5)
await bingx.connect()
bingx_task = asyncio.create_task(bingx.listen())

# Получить лучшую цену покупки
best_bid = bingx.get_best_bid()
best_ask = bingx.get_best_ask()
```

**WebSocket URL:** `wss://open-api-ws.bingx.com/market`

**Формат подписки:**
```json
{
  "id": "unique_id",
  "reqType": "sub",
  "dataType": "BTC-USDC@depth5"
}
```

**Формат ответа (gzip compressed):**
```json
{
  "code": 0,
  "data": {
    "bids": [[95823.09, 0.000409], [95789.79, 0.160254]],
    "asks": [[95862.56, 0.010432], [95861.81, 0.212867]]
  }
}
```

---

### 2. MEXC WebSocket

**Библиотека:** `ccxt.pro`

**Особенности:**
- ✅ Использует ccxt.pro для WebSocket
- ✅ Автоматическое управление соединением
- ✅ Стандартизированный API
- ✅ Встроенная обработка ошибок

**Пример использования:**
```python
import ccxt.pro as ccxtpro

mexc = ccxtpro.mexc()
orderbook = await mexc.watch_order_book('BTC/USDC', limit=5)

# Получить цены
ask = orderbook['asks'][0][0]  # Лучшая цена продажи
bid = orderbook['bids'][0][0]  # Лучшая цена покупки
```

**Внутренний WebSocket URL:** `wss://wbs.mexc.com/ws`

**Почему ccxt.pro?**
1. MEXC spot WebSocket использует **protobuf** для orderbook
2. Прямая подписка требует компиляции `.proto` файлов
3. ccxt.pro абстрагирует сложность и предоставляет единый API
4. Автоматическая обработка переподключений

---

## 📊 Производительность

| Метрика | BingX WS | MEXC (ccxt.pro) |
|---------|----------|-----------------|
| **Задержка** | ~10-50ms | ~50-100ms |
| **Частота обновлений** | На каждое изменение | На каждое изменение |
| **Reconnect** | Автоматический | Автоматический |
| **Стабильность** | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ |
| **Сжатие данных** | gzip | Нет |

---

## 🎯 Структура проекта

```
arbitrage-bot/
├── arbitrage_scanner_ws.py      # Главный скрипт WebSocket сканера
├── tools/
│   ├── bingx_orderbook_live.py  # BingX WebSocket класс
│   └── mexc_orderbook_live.py   # MEXC REST (устаревший)
├── src/
│   └── exchanges/
│       └── mexc_ws_port/         # Protobuf реализация (альтернатива)
│           ├── mexc_client.py
│           └── proto/
├── config/
│   └── .env                      # API ключи (опционально)
├── docs/
│   └── WEBSOCKET_IMPLEMENTATION.md
└── requirements.txt
```

---

## ⚙️ Настройки

**В файле `arbitrage_scanner_ws.py`:**

```python
# ========== НАСТРОЙКИ ==========
MIN_PROFIT_USD = Decimal("0.10")      # Минимальная прибыль для алерта ($)
TARGET_VOLUME_USDC = Decimal("100")   # Объём сделки в USDC
UPDATE_DISPLAY_EVERY = 50             # Показывать каждые N обновлений
# ================================
```

**Рекомендуемые значения:**

| Параметр | Тестирование | Продакшн |
|----------|--------------|----------|
| `MIN_PROFIT_USD` | 0.10 | 1.00 |
| `TARGET_VOLUME_USDC` | 100 | 500-1000 |
| `UPDATE_DISPLAY_EVERY` | 10 | 50-100 |

---

## 🔍 Отладка

### Проверка WebSocket подключений

**BingX:**
```bash
python tools/bingx_orderbook_live.py
```

Ожидаемый вывод:
```
🔌 Подключение к BingX WebSocket...
✅ Подключено к BingX WebSocket
📊 Подписка на order book для BTC-USDC (🌊 depth=5)

🔴 SELL   | $   95,862.56 | 0.010432 | $    999.95
🔴 SELL   | $   95,861.81 | 0.212867 | $ 20,400.12
...
```

**MEXC (через ccxt.pro):**
```python
import asyncio
import ccxt.pro as ccxtpro

async def test():
    mexc = ccxtpro.mexc()
    book = await mexc.watch_order_book('BTC/USDC')
    print(f"Ask: {book['asks'][0]}")
    print(f"Bid: {book['bids'][0]}")
    await mexc.close()

asyncio.run(test())
```

---

## 📈 Примеры вывода

### Нормальная работа:
```
==================================================================================
                            BTC/USDC ARBITRAGE SCANNER
==================================================================================

⚙️  Настройки:
   • Минимальная прибыль: $0.10
   • Объём: 100 USDC
   • Обновление вывода: каждые 50 тиков

----------------------------------------------------------------------------------
   Время   | Статус  |  Направление  |   Прибыль   | Цена покупки  | Цена продажи |     BTC      |  Тик  
----------------------------------------------------------------------------------

[BingX] Подключение WebSocket...
✅ Подключено к BingX WebSocket
[MEXC] Подключение через ccxt.pro WebSocket...

 19:57:32  |  ⚪ НЕТ  |  MEXC→BingX   |   $ -0.01   | MEXC $94249.57| BingX $94235.97|  0.001061 BTC|   50
 19:57:35  |  ⚪ НЕТ  |  BingX→MEXC   |   $ -0.02   | BingX $94250.10| MEXC $94235.00|  0.001060 BTC|  100
```

### При находке возможности:
```
 20:05:15  |✅ НАЙДЕНО| MEXC→BingX   |   $  0.15   | MEXC $94200.00| BingX $94350.50|  0.001062 BTC|  523

!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!
                        🎯 ВОЗМОЖНОСТЬ АРБИТРАЖА #1                              
!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!
   Направление: MEXC→BingX
   Покупка: 0.001062 BTC на MEXC за $94200.00
   Продажа: 0.001062 BTC на BingX за $94350.50
   Ожидаемая прибыль: $0.15
!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!
```

---

## ⚠️ Важные замечания

### 1. MEXC WebSocket через ccxt.pro

**Почему не нативный WebSocket?**
- MEXC spot orderbook использует **Google Protobuf** формат
- Требуется компиляция `.proto` файлов: `mexc_deals.proto`
- Нужна библиотека `protobuf` и генерация Python классов
- ccxt.pro инкапсулирует всю сложность

**Альтернатива (для продвинутых):**
В проекте есть `src/exchanges/mexc_ws_port/mexc_client.py` - кастомная реализация с protobuf.

### 2. BingX WebSocket

**Сжатие gzip:**
```python
import gzip
data = json.loads(gzip.decompress(message).decode())
```

**Reconnect:**
Класс автоматически переподключается при обрыве.

### 3. Оба соединения работают одновременно

```python
# BingX обновляется в фоновом task
bingx_task = asyncio.create_task(bingx.listen())

# MEXC обновляется в основном цикле
while True:
    mexc_book = await mexc.watch_order_book('BTC/USDC')
    # ... расчёт арбитража
```

---

## 🐛 Решение проблем

### Ошибка: `ModuleNotFoundError: No module named 'ccxt.pro'`

**Решение:**
```bash
pip uninstall ccxt
pip install ccxt[pro]
```

### BingX WebSocket не подключается

**Проверка:**
```bash
python tools/bingx_orderbook_live.py
```

**Возможные причины:**
- Firewall блокирует WebSocket
- Сетевые ограничения
- Проблемы на стороне биржи

### MEXC возвращает старые данные

**Причина:** ccxt.pro кеширует данные.

**Решение:** Используйте `limit` параметр:
```python
await mexc.watch_order_book('BTC/USDC', limit=5)
```

---

## 📝 История изменений

**2025-11-16:**
- ✅ Реализован WebSocket для BingX (кастомный класс)
- ✅ Добавлен MEXC WebSocket через ccxt.pro
- ✅ Создан финальный сканер с обоими WebSocket
- ✅ Добавлена проверка обоих направлений арбитража
- ✅ Реализован вывод статистики и алертов
- ✅ Документация WebSocket архитектуры

---

## 🎯 Дальнейшее развитие

### Планируется:
- [ ] Логирование в файл с ротацией
- [ ] Telegram уведомления при находке
- [ ] Автоматическое исполнение сделок
- [ ] Мониторинг комиссий в реальном времени
- [ ] Web Dashboard с графиками (Flask/FastAPI)
- [ ] Добавить больше бирж (Binance, Bybit)
- [ ] Триангулярный арбитраж

### В разработке:
- [ ] Интеграция с базой данных для статистики
- [ ] ML модель для предсказания возможностей
- [ ] Risk management система

---

## 📚 Полезные ссылки

### Документация API:
- [BingX WebSocket API](https://bingx-api.github.io/docs/#/en-us/spot/socket/market.html)
- [MEXC WebSocket API](https://mexcdevelop.github.io/apidocs/spot_v3_en/#websocket-market-data)
- [ccxt.pro Documentation](https://docs.ccxt.com/en/latest/ccxt.pro.manual.html)

### Библиотеки:
- [websockets](https://websockets.readthedocs.io/)
- [ccxt](https://github.com/ccxt/ccxt)
- [python-dotenv](https://pypi.org/project/python-dotenv/)

---

## 📧 Поддержка

**Проект:** arbitrage-bot  
**GitHub:** https://github.com/shopnextcryotech/phu  
**Дата создания:** 2025-11-16  

---

## 📄 Лицензия

MIT License - смотрите файл LICENSE для деталей.

---

**Made with ❤️ for cryptocurrency arbitrage traders**